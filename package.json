{
  "name": "nuclear-plant-sim",
  "version": "0.1.0",
  "description": "2D multiplayer nuclear power plant simulator",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test server/**/*.test.js",
    "serve": "npx serve ../.. -p 3000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "phaser": "^3.60.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
